import 'package:flutter/material.dart';

class PresentationContents extends StatelessWidget {
  const PresentationContents({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('chookity pak!'),
    );
  }
}